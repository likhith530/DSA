#include<stdio.h>
struct Node{
    struct Node* left;
    struct Node* right;
    int value;
};
void Insert(int key, struct Node* root)
{
    struct Node* t = root;
    struct Node*r, *p;
    if( root == NULL)
    {
        p = 
    }
}

int main(
    return 0;
)